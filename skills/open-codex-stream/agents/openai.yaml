interface:
  display_name: "Open Codex Stream"
  short_description: "Open a browser or terminal Codex Stream viewer"
  default_prompt: "Use $open-codex-stream to open the default Codex Stream viewer to the right, or its terminal fallback when I explicitly ask for TUI mode."
